const router = require("./notification.route");
const controller = require('./notification.controller');
const model = require('./notification.model');

module.exports = { router, controller, model };