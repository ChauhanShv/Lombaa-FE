const router = require("./user.package.route");
const controller = require('./user.package.controller');
const model = require('./user.package.model');

module.exports = { router, controller, model };